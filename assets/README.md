# Pass card design

**Source of truth (do not redesign):** [`../param-parivar-pass.html`](../param-parivar-pass.html)

- Preview: open that file, or [`../design.html`](../design.html) for live QR
- Print uses the same card markup via `pass-html.js` + `pass.css` (copied from the source file; only the QR slot is filled)
